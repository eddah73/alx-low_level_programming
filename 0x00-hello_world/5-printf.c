#include <stdio.h>

/**
 * main - strings printed by printf
 * followed by newline
 * Return: alway 0 on (success)
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
